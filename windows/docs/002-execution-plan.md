# 002 — 执行计划

**项目**：历史粘贴板 Windows 版
**创建日期**：2026-07-22
**状态**：Phase 0 进行中

---

## 总体策略

采用**分层推进**策略：先骨架后血肉，每个 Phase 产出可验证的成果。每完成一个 Phase 立即写入开发者日志。

## Phase 列表

### Phase 0：环境准备与文档搭建 ✅ (进行中)

| 任务 | 状态 |
|------|------|
| 安装 .NET SDK 9.0 | ✅ 完成 |
| 创建项目目录结构 | ✅ 完成 |
| 编写技术规范 (001) | ✅ 完成 |
| 编写执行计划 (002) | 本文件 |
| 编写需求文档 (003) | ⬜ |
| 创建 CLAUDE.md 项目指引 | ✅ 完成 |
| 创建第一个 devlog 条目 | ⬜ |
| 创建 .csproj 项目文件 | ⬜ |
| 复制图标资源并转换为 .ico | ⬜ |

**验证标准**：`dotnet restore` 成功（需在 Windows 上验证）

---

### Phase 1：项目骨架 — 托盘图标 + 右键菜单

| 任务 | 优先级 |
|------|--------|
| 创建 App.xaml/.cs（隐藏主窗口、无任务栏图标） | P0 |
| 创建 TrayIconManager（系统托盘 + 右键菜单） | P0 |
| 右键菜单：设置 / 关于 / 退出 | P0 |
| 创建主项目文件和基本结构（Models/Services/UI/Utils 空目录） | P0 |
| 设计 Windows 托盘图标（嵌入代码绘制或使用 .ico） | P1 |

**验证标准**：运行后系统托盘出现图标，右键弹出三个菜单项，“退出”可正常退出

---

### Phase 2：数据层 — SQLite 数据库 + 配置

| 任务 | 优先级 |
|------|--------|
| 创建 ClipboardItem 模型 | P0 |
| 创建 DatabaseService（建表、CRUD、清理） | P0 |
| 创建 SettingsManager（JSON 读写） | P0 |
| 保留天数（1/3/5 天）设置 | P1 |
| 开机自启设置（注册表） | P1 |

**验证标准**：可以通过代码创建数据库、插入记录、查询记录、清理过期数据

---

### Phase 3：剪贴板监控服务

| 任务 | 优先级 |
|------|--------|
| 创建 ClipboardMonitor（500ms 定时器轮询） | P0 |
| 文字剪贴板检测（UnicodeText / Text 格式） | P0 |
| 图片剪贴板检测（Bitmap / DIB / PNG） | P0 |
| 文件复制检测（FileDropList → 图片文件） | P0 |
| SHA256 哈希去重 | P0 |
| 图片保存到磁盘（PNG 格式） | P0 |
| ignoreOnce 机制（防止自触发） | P0 |
| 每 50 条自动清理 | P1 |

**验证标准**：复制文字/图片后，数据库中有新记录，图片文件保存在正确路径

---

### Phase 4：UI 面板

| 任务 | 优先级 |
|------|--------|
| 创建 MainWindow（无边框窗口，从托盘上方弹出） | P0 |
| 搜索框 + 实时过滤 | P0 |
| 卡片列表（虚拟化滚动） | P0 |
| ClipboardCard 控件（文字预览、图片缩略图、时间） | P0 |
| 置顶按钮、删除按钮 | P0 |
| 悬停效果（背景变色、删除按钮变红） | P1 |
| 底部状态栏（共 X 条 · 保留 X 天 + 齿轮按钮） | P0 |
| 单击卡片 → 复制 + 关闭面板 | P0 |
| 点击面板外部自动关闭 | P0 |
| 空状态提示 | P1 |
| SettingsWindow（设置窗口） | P0 |

**验证标准**：单击托盘图标弹出面板、搜索正常、卡片可交互、设置窗口正常

---

### Phase 5：安装器与收尾

| 任务 | 优先级 |
|------|--------|
| Inno Setup 脚本（.iss） | P0 |
| 生成 .ico 图标（16/32/48/256） | P0 |
| README.md（Windows 版用户文档） | P1 |
| 更新 CLAUDE.md | P1 |
| 端到端测试 | P0 |

**验证标准**：安装器生成的可执行文件可安装、运行、开机自启正常工作

---

## 依赖关系

```
Phase 0 (环境+文档)
    │
    v
Phase 1 (项目骨架)
    │
    v
Phase 2 (数据层)
    │
    v
Phase 3 (剪贴板监控)
    │
    v
Phase 4 (UI 面板)
    │
    v
Phase 5 (安装器)
```

每个 Phase 依赖前一个 Phase 完成。Phase 内任务可按 P0 → P1 顺序执行。

---

## 版本历史

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-22 | 1.0.0 | 初稿 |
